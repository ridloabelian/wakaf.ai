{
  "name": "wakaf-ai-ai-gateway",
  "version": "1.0.0",
  "description": "AI Gateway for wakaf.ai - LLM proxy with caching and fallback",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240614.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0",
    "vitest": "^1.6.0"
  }
}